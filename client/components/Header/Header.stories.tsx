import React from 'react';
import Header from './Header';

export default {
  title: 'Header',
};

export const HeaderWithNavMenu = () => (
  <Header withNavMenu />
);

export const HeaderDemoPage = () => (
  <Header demoPage />
);
